// This stub exists only to satisfy imports during build.
// Supabase is not used in this project.

// Export a minimal placeholder so "import { supabase } from '@/integrations/supabase/client'" won't fail if present anywhere.
export const supabase: any = null;
